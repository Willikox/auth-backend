# --- Base ---
PORT=3000
APP_URL=http://localhost:3000

# --- Base de datos (Postgres) ---
DATABASE_URL="postgresql://authuser:authpass@localhost:5432/authdb?schema=public"
SHADOW_DATABASE_URL="postgresql://authuser:authpass@localhost:5432/authdb_shadow?schema=public"

# --- JWT ---
JWT_SECRET="CHANGE_ME_super_secret_jwt"
JWT_ALG="HS256"
JWT_ACCESS_EXPIRES="15m"
JWT_REFRESH_EXPIRES="30d"

# --- BCrypt ---
BCRYPT_SALT_ROUNDS=12

# --- Redis (opcional, para rate limit / cache). Si no lo usarán, pueden comentar REDIS_URL. ---
REDIS_URL="redis://localhost:6379"

# --- SMTP (MailDev local por defecto) ---
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=

# --- Admins ---
ADMIN_EMAILS=admin@will.com